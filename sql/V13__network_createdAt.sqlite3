
ALTER TABLE network ADD COLUMN createdAt datetime;

UPDATE network SET createdAt = datetime();

CREATE TABLE partner_link (
    handleId TEXT NOT NULL,
    linkCode VARCHAR(32) NOT NULL,
    createdAt DATETIME NOT NULL,
    externalId VARCHAR(256),
    data TEXT,
    FOREIGN KEY (handleId) REFERENCES jwt(handleId)
);

