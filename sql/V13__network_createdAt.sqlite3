-- See README.md in this directory for explanations.

ALTER TABLE network ADD COLUMN createdAt datetime;

UPDATE network SET createdAt = datetime();

CREATE TABLE partner_link (
    jwtId VARCHAR(26),
    linkCode VARCHAR(32),
    data TEXT,
    FOREIGN KEY (jwtId) REFERENCES jwt(id)
);
